# ArchiveFS v0.6.0-alpha Release & Architecture Audit (refreshed)

> **Historical release audit**
>
> This audit records an earlier release-preparation state and is retained for provenance. It is not current architecture or capability guidance. See the [README](../README.md), [release engineering guide](RELEASE_ENGINEERING.md), and [CHANGELOG](../CHANGELOG.md) for current guidance.

**Original audit date:** 2026-07-23 (branch `sonnet-v0.6-release-audit`,
`origin/main` at `494f826`)
**First refresh:** 2026-07-23 (branch `sonnet-v0.6-docs-cleanup`,
`origin/main` at `268d37a`, five commits later - the RetroArch trusted
catalogue download/management feature)
**Second refresh (this task):** release-prep audit on branch `main` at
`49dc989` (80 commits past `v0.5.0-alpha`, 61 commits past the first
refresh's `268d37a`). **Method:** direct `git log`/`grep` verification
against the actual current tree, not assumption from prior document text -
every claim below that changed from the first refresh was independently
re-checked (commit range, `grep` for the literal stale string, and the
actual current constant values). No files outside documentation and
`Cargo.toml`/`Cargo.lock` were changed as part of this pass.

This refresh **removes findings that this branch has already resolved**
and **does not mark any of Codex's in-progress parallel work as
complete**. It supersedes the original audit's blocker table and go/no-go
checklist; the original's detailed technical evidence (safety-property
verdicts, adapter readiness matrix, performance/maintainability sections)
remains valid and is carried forward rather than re-derived, since the
underlying code it evaluated has not changed in this window.

**What changed since the first refresh (`268d37a` → `49dc989`):** the four
items the first refresh listed as "Codex in progress, not yet merged" -
the RetroArch catalogue parse-tolerance issue, the stale Cheats & Mods
"Stage 3" GUI copy, the Libretro archive size-limit handling, and "usable
with warnings" catalogue behavior - **have all landed on `main`**,
confirmed via `git log 268d37a..49dc989` (commits `7af8a5c`, `824bd34`,
`e31a399`, `947686b`, `f65da2a`/`b44f585`) and direct source inspection
(`CatalogueIndexState::UsablePartial`/`excluded_entries` in
`cheat_catalogue.rs`; `CHEAT_SOURCE_FILE_SIZE_LIMIT`/
`CHEAT_SOURCE_EXPANDED_SIZE_LIMIT` = 256 MiB/1 GiB in `cheat_sources.rs`;
the stale string is no longer present anywhere in `main.rs`'s actual
rendering code, only as a negative test assertion). **A separate,
unrelated 69-commit development stack (Dolphin upstream catalogue, Xenia
Canary provider, GameCube direct-image identity, platform registry)
exists on unmerged local branches and was deliberately left untouched -
it is out of scope for `v0.6.0-alpha` and targets a later release.**

---

## 1. Current release readiness

**Documentation blockers from the original audit are resolved.**
`CHANGELOG.md` now has a complete `v0.6.0-alpha` section; `README.md`,
`ROADMAP.md`, and the shared safety docs no longer describe Dolphin as
unmerged or the RetroArch GUI apply bridge as deferred; a new adapter
support matrix and consolidated v0.6 manual QA plan exist.

**Code blockers from the first refresh are now fixed on `main`** (see
"What changed" above) - the catalogue parse-tolerance issue, the stale GUI
copy, and the archive size-limit/usable-with-warnings handling are all
confirmed merged as of `49dc989`.

**One thing still blocks a credible `v0.6.0-alpha` tag:**

1. **No real manual Nobara QA pass has been recorded** against
   `docs/MANUAL_QA_v0.6.0-alpha.md`. This remains the sole blocking gap -
   none of this release's safety/GUI work has been exercised on real
   target hardware yet, only validated by automated tests and source
   review.

Everything else from the original audit's GUI-usability and
maintainability findings (Section 4 below) remains open at the same
severity as originally assessed; none of it is a release blocker.

## 2. Remaining blockers

| # | Title | Severity | Status |
|---|---|---|---|
| 1 | Real manual Nobara QA pass against `docs/MANUAL_QA_v0.6.0-alpha.md` | Blocker | **Open** - not yet run |
| 6 | Silent apply-failure reset to `Idle` with no on-page explanation | High | Not independently re-verified this pass; carried forward open from the original audit |
| 7 | Cheats & Mods page doesn't use the shared scrollable-page keyboard wrapper | Medium | Confirmed still accurate (`CHANGELOG.md`'s own "Changed" entry documents this); not a release blocker |
| 8 | "Open Cheats & Mods" button gives no reason when disabled purely by `busy` | Medium | Not independently re-verified this pass; carried forward open |
| 9 | Cheats/patches/mods vocabulary inconsistent across adapters | Medium | Documented explicitly in `docs/ADAPTER_SUPPORT_MATRIX.md`'s "Cheats, patches, and mods are not the same thing" section as an accepted stopgap; not a blocker |
| 10 | No AppImage/`.desktop`/icon packaging | Medium | Open, not a v0.6 blocker |
| 11 | Raw `{:?}`-formatted enum text and internal jargon in end-user UI copy | Low | Not independently re-verified this pass; carried forward open |
| 12 | No crash-orphan cleanup for a temp file/journal on a hard process kill mid-write | Medium | Open, undocumented gap (not a broken promise - no doc claims otherwise) |

**Resolved since the first refresh (no longer blockers):**

- ~~RetroArch catalogue parse-tolerance issue (malformed entries could
  affect whole-snapshot validation)~~ - fixed: `CatalogueIndexState::UsablePartial`
  and per-entry `excluded_entries` tracking confirmed in
  `cheat_catalogue.rs`.
- ~~Stale "Archive matching and cheat installation are not yet
  implemented" GUI copy~~ - fixed: confirmed absent from `main.rs`'s
  rendering code via direct `grep`; only appears as a negative test
  assertion string.
- ~~Libretro catalogue archive size-limit handling~~ - fixed: per-entry
  limit raised to 256 MiB, expanded-archive limit raised to 1 GiB
  (`cheat_sources.rs` constants), confirmed at current values.
- ~~"Usable with warnings" catalogue behavior~~ - fixed: same
  `UsablePartial`/`excluded_entries` mechanism as above.

**Resolved by the first refresh (no longer blockers):**

- ~~`CHANGELOG.md` has no entry for post-`v0.5.0-alpha` work~~ - fixed: a
  complete `v0.6.0-alpha` section now exists.
- ~~`CHANGELOG.md`/`README.md`/`ROADMAP.md` claim Dolphin is "not yet
  merged"~~ - fixed in all three files; corrected to reflect that Dolphin
  was actually merged before the `v0.5.0-alpha` tag was even cut.
- ~~Shared docs claim the RetroArch GUI materialization bridge "remains
  deferred"~~ - fixed in `docs/SHARED_CHEAT_PREVIEW.md` and
  `docs/SHARED_SAFE_APPLY_ROLLBACK.md`.
- ~~`docs/RELEASE_NOTES_v0.5.0-alpha.md` claims cheat apply is CLI-only~~ -
  corrected with a historical-context note (accurate for v0.5.0-alpha at
  the time, explicitly marked superseded).
- ~~`README.md` self-contradicts about Dolphin's status~~ - fixed; both
  halves of the file now agree.
- ~~No adapter support matrix exists~~ - fixed:
  `docs/ADAPTER_SUPPORT_MATRIX.md` created.
- ~~No consolidated v0.6 manual QA plan exists~~ - fixed:
  `docs/MANUAL_QA_v0.6.0-alpha.md` created (though not yet *run*, per
  Blocker 1 above).
- ~~No `docs/RELEASE_NOTES_v0.6.0-alpha.md` exists~~ - fixed, and it
  correctly does not claim v0.6.0-alpha has shipped.

## 3. Documentation blockers resolved by this branch

Full detail in this branch's own commits (`fce3f1e`, `2d73188`); summary:

- `CHANGELOG.md` restructured: `v0.5.0-alpha` closed out as released
  (Dolphin folded into its `Added` list, "Pending merge" section removed
  entirely since nothing in it was actually pending anymore); new
  `v0.6.0-alpha (unreleased)` section added with Added/Changed/Security/
  Known-limitations covering every commit since the tag, without listing
  every individual internal commit.
- `README.md`: release-status banner corrected; new "does today" bullets
  for RetroArch GUI apply/rollback, catalogue download/management,
  Recently Found, and Mega Drive detection; stale `v0.4.3-alpha` install
  example updated to `v0.5.0-alpha`.
- `ROADMAP.md`: all genuinely-merged work moved into "Completed
  foundations"; "Current development" rewritten to describe only Codex's
  actual in-progress, not-yet-merged work; "Next milestones" rewritten in
  the requested priority order with concrete detail on why PCSX2/Dolphin
  section-selection is a real, distinct piece of work (a single `.pnach`
  or Dolphin INI can contain multiple independently selectable
  patches/cheats); future-adapter list updated to PPSSPP/RPCS3/Xenia/
  Switch/MAME/WHDLoad with Switch flagged as needing a separate policy
  decision, not just an architecture review.
- `docs/ADAPTER_SUPPORT_MATRIX.md` created.
- `docs/SHARED_CHEAT_PREVIEW.md`, `docs/SHARED_SAFE_APPLY_ROLLBACK.md`:
  stale "deferred"/"future work" RetroArch-bridge language removed and
  replaced with an accurate "implemented and wired" statement, cross-
  referencing `docs/RETROARCH_GUI_APPLY_HISTORY.md`.
- `docs/RELEASE_NOTES_v0.5.0-alpha.md`, `docs/MANUAL_QA_v0.5.0-alpha.md`:
  corrected as historical documents (Dolphin's "pending merge" framing was
  factually wrong even at v0.5.0-alpha's own tag time, so this was a
  correction, not a rewrite of true history; the CLI-only cheat-apply
  claim was true at the time and is now marked superseded, not deleted).
- `docs/MANUAL_QA_v0.6.0-alpha.md`, `docs/RELEASE_NOTES_v0.6.0-alpha.md`
  created new.

## 4. Manual QA blockers

- **No Nobara pass recorded against `docs/MANUAL_QA_v0.6.0-alpha.md`.**
  This is Blocker 1 above and the single largest remaining gap before a
  credible `v0.6.0-alpha` tag - none of the new safety/GUI work in this
  release has been exercised on real target hardware yet, only validated
  by automated tests and source review.
- The v0.5.0-alpha manual QA plan's own Dolphin section was, at the time
  it was written, marked conditional on a not-yet-merged branch; this
  refresh confirms (via `git merge-base`) that Dolphin was actually merged
  before the `v0.5.0-alpha` tag, so that historical plan's Dolphin
  coverage should be treated as unconditionally applicable, not skipped.

## 5. Current RetroArch catalogue issue

**Confirmed fixed on `main` at `49dc989` as of this second refresh.**
`git log 268d37a..49dc989` shows `7af8a5c` ("Fix RetroArch catalogue
tolerance and matching"), `824bd34` ("Restore RetroArch matching and
update catalogue bounds"), and `e31a399` ("Distinguish fatal snapshot
failures from excluded catalogue entries") landed after the first refresh.
`cheat_catalogue.rs` now defines `CatalogueIndexState::UsablePartial` and
`excluded_entries` tracking, confirmed by direct source read, not assumed.
The related stale-GUI-wording issue is also fixed: the literal string
"Archive matching and cheat installation are not yet implemented" no
longer appears anywhere in `main.rs`'s rendering code (`grep` finds it only
inside a test's negative assertion, which proves the *absence* of the
string in real output). `CHANGELOG.md`, `ROADMAP.md`,
`docs/RELEASE_NOTES_v0.6.0-alpha.md`, `docs/MANUAL_QA_v0.6.0-alpha.md`, and
`docs/ADAPTER_SUPPORT_MATRIX.md` have all been updated to describe this as
fixed rather than open.

## 6. PCSX2/Dolphin preview-only status

**Confirmed unchanged and accurate.** Neither adapter has an independent,
approved local source artifact to apply from - PCSX2 inventories only
emulator-managed PNACH files, Dolphin only emulator-managed GameSettings
INIs. Neither has any Install/Apply/Enable/Disable/Rollback control
anywhere in the GUI. This is a scope decision (no official upstream
provider integrated yet, see `ROADMAP.md`'s "Next milestones" 2-5), not a
bug, and is now consistently described that way across `CHANGELOG.md`,
`README.md`, `docs/ADAPTER_SUPPORT_MATRIX.md`, and
`docs/RELEASE_NOTES_v0.6.0-alpha.md`.

## 7. Carried-forward findings from the original audit (unchanged)

These were out of scope for this documentation-only branch and remain
exactly as originally assessed - see the original audit's full text
(preserved in this branch's git history at commit prior to this refresh,
and summarized in Section 2's table above) for complete file:line
evidence:

- Core safety review (16 properties checked against `shared_transaction.rs`,
  `shared_preview.rs`, `destination_safety.rs`, `dolphin_local.rs`,
  `pcsx2_local.rs`, `cheat_cache_lock.rs`, `game_identity.rs`): all match
  their documented guarantees, with two precision notes (a low-level
  `destination_safety.rs` helper would itself allow a relative path if
  called directly, though no real caller does; two network paths exist in
  the crate, not one - the RetroArch catalogue fetch and a separate
  legacy CLI-only PCSX2 metadata fetch, neither reachable from apply/
  rollback code).
- Adapter readiness matrix (RetroArch/PCSX2/Dolphin, unchanged in this
  window except that RetroArch's catalogue-manager GUI work landed
  exactly as expected and does not change its Apply-readiness verdict,
  which was already "ready and wired").
- GUI usability findings 6-11 in the original audit (silent failure
  reset, inconsistent scrollable page, unexplained disabled control,
  terminology drift, developer-facing text, no cancellation mid-write).
- Performance risks (Recently Found's 10,000-entry bound, the 512-journal
  history scan bound, unbounded backup/journal retention over time,
  immutable-mode GUI rendering at scale, database migration cost at
  scale) - all unchanged, none newly triggered by the catalogue-manager
  commits.
- Maintainability risks (`main.rs` at 34,804+ lines with only a 327-line
  shared `ui` module; adapter-specific eligibility/destination rules
  embedded directly in the shared preview module rather than behind a
  per-adapter strategy boundary; `#[allow(clippy::too_many_arguments)]`
  appearing 4 times) - all unchanged.

Current test/lint status (re-verified for this second refresh at `49dc989`):
`cargo fmt --all -- --check` clean; `cargo clippy --workspace
--all-targets -- -D warnings` clean; `cargo test --workspace` reports
**1,499 tests passing** (CLI 127, core 907, GUI 465) - up from 1,407 at
the first refresh, reflecting the catalogue-tolerance, size-limit, and
GUI-redesign/Library-tabs/Sources-cleanup commits' own test additions
between `268d37a` and `49dc989`.

## 8. Go/no-go checklist for v0.6.0-alpha

- [x] `CHANGELOG.md` has a complete `v0.6.0-alpha` section covering all
      work since `v0.5.0-alpha`.
- [x] Every "Dolphin not yet merged" / "pending merge" sentence is removed
      from `CHANGELOG.md`, `README.md`, and `ROADMAP.md`.
- [x] `docs/SHARED_CHEAT_PREVIEW.md` and `docs/SHARED_SAFE_APPLY_
      ROLLBACK.md` no longer describe the RetroArch GUI materialization
      bridge as deferred/future work.
- [x] `docs/RELEASE_NOTES_v0.5.0-alpha.md`'s stale Known-limitations claim
      is corrected with a historical-context note.
- [x] `README.md`'s internal self-contradiction about Dolphin is resolved.
- [x] An adapter support matrix exists
      (`docs/ADAPTER_SUPPORT_MATRIX.md`).
- [x] A consolidated `docs/MANUAL_QA_v0.6.0-alpha.md` exists.
- [x] A `docs/RELEASE_NOTES_v0.6.0-alpha.md` exists and does not claim the
      release has shipped.
- [ ] **A real Nobara manual QA pass has been run against
      `docs/MANUAL_QA_v0.6.0-alpha.md`, with results recorded.**
- [x] The RetroArch catalogue parse-tolerance fix has merged into `main`
      (confirmed at `49dc989`).
- [x] The stale "not yet implemented" Cheats & Mods GUI copy fix has
      merged into `main` (confirmed at `49dc989`).
- [x] Libretro archive size-limit handling and "usable with warnings"
      catalogue behavior have merged into `main` (confirmed at `49dc989`).
- [ ] Findings 6 (silent failure reset) and 7 (Cheats & Mods scrolling)
      from the original audit are resolved or explicitly deferred with a
      tracking note. (Deferred: neither is a release blocker per Section 2's
      severity ratings; finding 7 is accurately documented in `CHANGELOG.md`
      rather than silently dropped.)
- [x] `cargo fmt --all -- --check`, `cargo clippy --workspace --all-
      targets -- -D warnings`, and `cargo test --workspace` are all green
      on the current commit (re-verified for this second refresh: 1,499
      tests passing, zero lint warnings).
- [x] Workspace version bumped to `0.6.0-alpha` in `Cargo.toml` (and
      `Cargo.lock` regenerated accordingly).
- [ ] `docs/release-checklist.md`'s Version/Git-and-CI steps are followed
      for the tag itself (version bump done; tag not yet pushed - no tag
      exists).
- [x] Working tree is clean and `git diff --check` passes on every commit
      in this branch.

**Overall: not ready to tag.** Every code-level blocker from both prior
refreshes is now resolved. **The single remaining item is a real Nobara
manual QA pass** - this is a hard release blocker per this task's
instructions and must not be waived; do not tag `v0.6.0-alpha` until it is
run and recorded. Findings 6-12 in Section 2 are lower-severity and may be
deferred with an explicit tracking note, but finding 1 (manual QA) may not.

---

*This second refresh modified `Cargo.toml`, `Cargo.lock`, `README.md`,
`CHANGELOG.md`, `ROADMAP.md`, `docs/ADAPTER_SUPPORT_MATRIX.md`,
`docs/MANUAL_QA_v0.6.0-alpha.md`, `docs/RELEASE_NOTES_v0.6.0-alpha.md`, and
this file. No other production code or documentation was changed. The
protected files `RETROARCH_ENV_DESIGN_DRAFT_v1.md` and `send-to-nobara.sh`
were never accessed, read, staged, or referenced.*

---

*The first refresh modified only `docs/V0.6_RELEASE_AUDIT.md`. No production
code, tests, or other documentation were changed as part of this specific
refresh (the other documentation corrections referenced above were made
in this branch's prior two commits, not this one). The protected file
`/home/davedap/archivefs/RETROARCH_ENV_DESIGN_DRAFT_v1.md` was never
accessed, read, or referenced.*
